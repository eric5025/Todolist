<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/js/home/todolist.js" defer></script>
    <title>To-Do List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #222;
            color: white;
            text-align: center;
        }
        .container {
            width: 300px;
            margin: auto;
            background: #333;
            padding: 20px;
            border-radius: 10px;
        }
        input, button {
            width: 80%;
            padding: 10px;
            margin: 5px;
        }
        .task {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #444;
            padding: 10px;
            border-radius: 5px;
            margin-top: 5px;
        }
        .completed {
            text-decoration: line-through;
            color: gray;
        }
        .task:hover {
            background: rgb(198, 234, 131);
            border-radius: 10%;
        }
    </style>
</head>
<body>
    <h1>To-Do List</h1>

    <div class="container">
        <input type="text" id="newTask" placeholder="할 일 입력">
        <button onclick="addTask()">추가</button>

        <div id="taskList">
            <!-- 할 일이 추가되는 공간 -->
        </div>
    </div>
</body>
</html>
